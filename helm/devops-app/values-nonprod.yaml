# NonProd — lower resources, single replica
replicaCount: 1

env:
  APP_ENV:     "nonprod"
  APP_VERSION: "1.0.0"

resources:
  requests:
    memory: "128Mi"
    cpu: "50m"
  limits:
    memory: "256Mi"
    cpu: "250m"

autoscaling:
  enabled: false
